# Database configuration for DbMate
DATABASE_URL=postgres://workpulse:supersecret@localhost:5432/workpulse?sslmode=disable

# Optional: Specify migrations directory (defaults to ./db/migrations)
# DBMATE_MIGRATIONS_DIR=./db/migrations

# Optional: Specify schema file location (defaults to ./db/schema.sql)
# DBMATE_SCHEMA_FILE=./db/schema.sql